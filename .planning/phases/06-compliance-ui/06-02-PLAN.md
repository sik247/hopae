---
phase: 06-compliance-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/compliance/entity-compliance-timeline.tsx
  - src/components/compliance/timeline-item.tsx
  - src/components/compliance/risk-dashboard.tsx
  - src/components/compliance/risk-entity-row.tsx
autonomous: true
requirements: [COMP-04, COMP-05]

must_haves:
  truths:
    - "User sees a risk dashboard with entities ranked by risk level"
    - "User can inspect per-entity compliance timeline showing each obligation's status"
    - "Critical entities appear at top of risk dashboard"
    - "Timeline shows overdue, due-soon, and compliant obligations distinctly"
  artifacts:
    - path: "src/components/compliance/risk-dashboard.tsx"
      provides: "Ranked entity list by risk level"
      min_lines: 40
    - path: "src/components/compliance/entity-compliance-timeline.tsx"
      provides: "Per-entity obligation timeline"
      min_lines: 40
    - path: "src/components/compliance/risk-entity-row.tsx"
      provides: "Single entity row in risk dashboard"
      min_lines: 20
    - path: "src/components/compliance/timeline-item.tsx"
      provides: "Single obligation in timeline"
      min_lines: 15
  key_links:
    - from: "src/components/compliance/risk-dashboard.tsx"
      to: "EntityRiskScore"
      via: "props from compliance engine"
      pattern: "EntityRiskScore|EntityScoreWithContext"
    - from: "src/components/compliance/entity-compliance-timeline.tsx"
      to: "DeadlineResult"
      via: "props from compliance engine"
      pattern: "DeadlineResult"
---

<objective>
Build the risk dashboard and per-entity compliance timeline components, then integrate them into the compliance page.

Purpose: The risk dashboard gives users an immediate ranked view of which entities need attention (COMP-04). The entity compliance timeline lets users drill into any entity's specific obligations (COMP-05). Together with Plan 01's calendar and alerts, this completes the compliance UI.

Output: Risk dashboard component with ranked entities, entity compliance timeline component, both integrated into /compliance page.
</objective>

<execution_context>
@/Users/harrykang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrykang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-compliance-ui/06-RESEARCH.md
@.planning/phases/06-compliance-ui/06-01-PLAN.md

@src/lib/compliance-engine/index.ts
@src/lib/compliance-engine/types.ts
@src/lib/db/types.ts
@src/components/entities/status-badge.tsx

<interfaces>
<!-- Types from compliance engine -->

```typescript
interface EntityRiskScore { entityId: string; entityName: string; riskLevel: RiskLevel; overdue: DeadlineResult[]; dueSoon: DeadlineResult[]; compliant: DeadlineResult[]; isDissolving: boolean }
interface DeadlineResult { requirementType: string; dueDate: Date; daysUntilDue: number; isOverdue: boolean; isDueSoon: boolean; fiscalYear: number }
interface EntityScoreWithContext { score: EntityRiskScore; entity: { id: string; name: string; legalName: string }; jurisdiction: { countryCode: string; countryName: string } }
type RiskLevel = 'critical' | 'warning' | 'ok'
```

<!-- compute-all.ts created in Plan 01 will need a new export -->
Plan 01 creates `src/lib/compliance-engine/compute-all.ts`. This plan adds a `computeAllRiskScores()` function that returns `EntityScoreWithContext[]` sorted by risk (critical first).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build risk dashboard and entity timeline components</name>
  <files>src/components/compliance/risk-dashboard.tsx, src/components/compliance/risk-entity-row.tsx, src/components/compliance/entity-compliance-timeline.tsx, src/components/compliance/timeline-item.tsx</files>
  <action>
    1. Create `src/components/compliance/risk-dashboard.tsx` ("use client"):
       - Props: `entityScores: SerializedEntityScoreWithContext[]` (dates as strings for serialization)
       - Renders a Card with header "Risk Dashboard" and count of entities with issues
       - Inside: scrollable list (max-h-[500px]) of RiskEntityRow components
       - Sort order: critical first, then warning, then ok
       - Add a summary bar at top showing: N critical (red), N warning (amber), N ok (green) as small badge counts
       - State: `selectedEntityId: string | null` — when an entity is clicked, show its timeline below the dashboard

    2. Create `src/components/compliance/risk-entity-row.tsx`:
       - Props: entity score context object, isSelected boolean, onSelect callback
       - Renders as a clickable row with: risk level indicator (colored left border or icon), entity name, country flag + code, counts (overdue: N, due soon: N)
       - Use same color scheme: red border for critical, amber for warning, green for ok
       - Highlight selected row with bg-muted

    3. Create `src/components/compliance/entity-compliance-timeline.tsx` ("use client"):
       - Props: `entityName: string`, `legalName: string`, `countryCode: string`, `deadlines: SerializedDeadlineResult[]` (all deadlines — overdue, dueSoon, compliant merged)
       - Renders a Card with header showing entity name and country flag
       - Inside: vertical timeline of TimelineItem components, sorted by dueDate ascending
       - Each item connected by a vertical line (Tailwind border-l)

    4. Create `src/components/compliance/timeline-item.tsx`:
       - Props: single deadline result (serialized)
       - Renders: colored dot on the timeline line (red if overdue, amber if dueSoon, green if compliant)
       - Content: requirement type (formatted — "Annual Filing", "Tax Return", "Agent Renewal"), due date formatted with date-fns (`format(date, "MMM d, yyyy")`), FY label, status text
       - Status text: "X days overdue" (red), "due in X days" (amber), or "compliant" (green)

    Serialization note: All Date objects must be passed as ISO strings from server components. Client components parse with `new Date(isoString)`.
  </action>
  <verify>
    <automated>cd /Users/harrykang/Desktop/hopae && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Risk dashboard renders ranked entity list with color-coded risk levels. Clicking an entity shows its compliance timeline. Timeline shows each obligation with status indicator.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate risk dashboard and timeline into compliance page</name>
  <files>src/lib/compliance-engine/compute-all.ts, src/app/compliance/page.tsx</files>
  <action>
    1. Update `src/lib/compliance-engine/compute-all.ts` (created in Plan 01):
       - Add export `computeAllRiskScores()` that returns `EntityScoreWithContext[]` sorted by risk level (critical first, then warning, then ok)
       - This function reuses the same Supabase fetch + engine computation pattern, but returns the intermediate risk scores instead of just alerts
       - If `computeAllDeadlines` and `computeAllAlerts` already compute risk scores internally, refactor to share the computation — do NOT fetch from Supabase twice

    2. Update `src/app/compliance/page.tsx`:
       - Add a tabbed interface using simple Tailwind button tabs (not a heavy tab library) with three tabs: "Calendar", "Risk Dashboard", "Alerts"
       - Make this a client component wrapper that receives all data from a parent server component, OR keep page.tsx as server component and create a CompliancePageClient for tab state
       - Calendar tab: shows ComplianceCalendar (from Plan 01)
       - Risk Dashboard tab: shows RiskDashboard component with entity scores, plus EntityComplianceTimeline below when an entity is selected
       - Alerts tab: shows AlertFeed (from Plan 01)
       - Default to Calendar tab
       - Serialize all Date objects to ISO strings when passing to client components

    Tab implementation: Use URL search params (`?tab=calendar|risk|alerts`) or simple useState. Prefer useState for speed — no server roundtrip on tab switch.
  </action>
  <verify>
    <automated>cd /Users/harrykang/Desktop/hopae && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>Compliance page has three tabs. Calendar shows deadlines, Risk Dashboard shows ranked entities with expandable timelines, Alerts shows ranked feed. All data computed server-side and serialized to client.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Visit `/compliance` — three tabs visible: Calendar, Risk Dashboard, Alerts
3. Risk Dashboard tab: entities ranked by risk level, critical entities at top
4. Click an entity in risk dashboard — its compliance timeline appears below
5. Timeline shows overdue (red), due-soon (amber), and compliant (green) obligations
6. Alert feed surfaces 3+ overdue and 5+ due-soon entities
</verification>

<success_criteria>
- Risk dashboard displays all entities ranked by risk level
- Per-entity compliance timeline renders with color-coded obligation statuses
- Compliance page has functional tab navigation between Calendar, Risk Dashboard, and Alerts
- All three views use consistent urgency color coding (red/amber/green)
- Page builds and renders without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-compliance-ui/06-02-SUMMARY.md`
</output>
