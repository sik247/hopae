---
phase: 07-dashboard
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/app/api/ai/briefing/route.ts
  - src/components/dashboard/ai-briefing.tsx
  - src/app/dashboard/page.tsx
autonomous: true
requirements: [DASH-03]

must_haves:
  truths:
    - "User can view an AI-generated compliance briefing on the dashboard"
    - "Briefing summarizes overdue filings, due-soon deadlines, and high-risk jurisdictions in natural language"
    - "Briefing falls back to a template summary if Gemini is unavailable"
  artifacts:
    - path: "src/app/api/ai/briefing/route.ts"
      provides: "Server-side Route Handler for Gemini briefing generation"
      exports: ["GET"]
    - path: "src/components/dashboard/ai-briefing.tsx"
      provides: "Client component that fetches and displays AI briefing"
      min_lines: 40
  key_links:
    - from: "src/components/dashboard/ai-briefing.tsx"
      to: "/api/ai/briefing"
      via: "fetch call"
      pattern: "fetch.*api/ai/briefing"
    - from: "src/app/api/ai/briefing/route.ts"
      to: "src/lib/compliance-engine/compute-all.ts"
      via: "server-side compliance data for prompt context"
      pattern: "computeAllAlerts|computeDashboardData"
---

<objective>
Add AI-generated compliance briefing to the dashboard using Gemini.

Purpose: The AI briefing transforms raw compliance data into a natural language summary that tells the portfolio health story in plain English — "2 overdue filings in JP and DE, 5 renewals due within 30 days." This is the AI demo showcase for the dashboard.

Output: Route Handler for Gemini briefing, client component that displays it, integrated into dashboard page.
</objective>

<execution_context>
@/Users/harrykang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrykang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard/07-RESEARCH.md
@.planning/phases/07-dashboard/07-01-PLAN.md

@src/lib/compliance-engine/types.ts
@src/lib/db/types.ts

<interfaces>
<!-- From Plan 01 compute-dashboard-data.ts -->
```typescript
interface DashboardData {
  summary: { totalEntities: number; atRisk: number; overdueFilings: number; upcomingDeadlines: number }
  jurisdictionRisks: { countryCode: string; countryName: string; entityCount: number; criticalCount: number; warningCount: number; worstRisk: RiskLevel }[]
  urgentActions: SerializedAlertItem[]
}
```

<!-- Gemini SDK (to be installed) -->
```typescript
import { GoogleGenAI } from '@google/genai'
const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY })
const response = await ai.models.generateContent({ model: 'gemini-2.0-flash', contents: prompt })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AI briefing Route Handler and client component</name>
  <files>src/app/api/ai/briefing/route.ts, src/components/dashboard/ai-briefing.tsx, src/app/dashboard/page.tsx</files>
  <action>
    1. Install `@google/genai` if not already present: `npm install @google/genai@1.45.0`
       Add `GEMINI_API_KEY` to `.env.example` if not already there.

    2. Create `src/app/api/ai/briefing/route.ts`:
       - Import `GoogleGenAI` from `@google/genai`
       - Import `computeDashboardData` from dashboard data layer
       - Export async GET handler:
         - Call `computeDashboardData()` to get summary, jurisdiction risks, urgent actions
         - Build a structured prompt for Gemini:
           ```
           You are a compliance analyst for Hopae, a company managing {totalEntities} legal entities across {jurisdictionCount} jurisdictions.

           Current compliance status:
           - {overdueFilings} overdue filings requiring immediate action
           - {upcomingDeadlines} deadlines due within 30 days
           - {atRisk} entities at risk

           High-risk jurisdictions: {list critical jurisdictions with counts}

           Most urgent items: {top 5 alerts with entity name, obligation, days}

           Write a concise 2-3 paragraph compliance briefing for the ops team. Be specific about which entities and jurisdictions need attention. Use a professional but direct tone. Do not use bullet points — write in prose.
           ```
         - Call Gemini `gemini-2.0-flash` model (fast, cheap)
         - Return JSON: `{ briefing: string, generatedAt: string }`
         - Error handling: if GEMINI_API_KEY missing or Gemini fails, generate a fallback template briefing using the same data:
           `"Your portfolio of {N} entities has {overdueFilings} overdue filings and {upcomingDeadlines} upcoming deadlines. {criticalCount} jurisdictions require immediate attention: {list}. Review the urgent actions below for specific filing details."`
         - Mark fallback with `{ briefing: string, generatedAt: string, isTemplate: true }`

    3. Create `src/components/dashboard/ai-briefing.tsx` ("use client"):
       - Uses `useEffect` + `useState` to fetch from `/api/ai/briefing` on mount
       - States: loading (skeleton), loaded (show briefing), error (show fallback message)
       - Render a Card with header "AI Compliance Briefing" and a sparkle icon (Sparkles from lucide-react)
       - Show a small "AI Generated" badge if not template, "Auto-generated summary" if template
       - Briefing text in a readable font size with proper paragraph spacing
       - Show "Generated at {time}" footer in muted text
       - Loading state: 3 Skeleton lines inside the card

    4. Update `src/app/dashboard/page.tsx`:
       - Add AiBriefing component below the health summary cards, above the heatmap/actions row
       - Full width card
       - This is a client component so it loads independently — won't block the server-rendered dashboard
  </action>
  <verify>
    <automated>cd /Users/harrykang/Desktop/hopae && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>Dashboard shows AI compliance briefing card. If GEMINI_API_KEY is set, shows AI-generated prose summary. If not, shows template-based fallback. Briefing references specific entities and jurisdictions from the data.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Visit `/dashboard` — AI briefing card appears with loading skeleton, then content
3. Without GEMINI_API_KEY: briefing shows template fallback with real data counts
4. With GEMINI_API_KEY: briefing shows natural language summary mentioning specific entities and jurisdictions
5. Briefing card does not block initial dashboard render (loads async)
6. "AI Generated" or "Auto-generated summary" badge is visible
</verification>

<success_criteria>
- AI briefing Route Handler at /api/ai/briefing returns structured response
- Client component fetches and displays briefing with loading state
- Fallback template works when Gemini unavailable
- Briefing references real compliance data (not generic text)
- Dashboard page renders all four sections: cards, briefing, heatmap, urgent actions
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard/07-02-SUMMARY.md`
</output>
