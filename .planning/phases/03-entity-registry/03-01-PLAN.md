---
phase: 03-entity-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/entities/page.tsx
  - src/components/entities/columns.tsx
  - src/components/entities/entity-data-table.tsx
  - src/components/entities/status-badge.tsx
autonomous: true
requirements: [ENTY-01]

must_haves:
  truths:
    - "User sees a dense table listing all 60+ entities on the /entities page"
    - "Each row shows entity name, jurisdiction, entity type, status badge, incorporation date"
    - "Status and risk badges use color coding (red, amber, green)"
    - "Table rows are clickable and navigate to /entities/[id]"
  artifacts:
    - path: "src/app/entities/page.tsx"
      provides: "Server Component fetching entity_health_summary from Supabase"
      contains: "entity_health_summary"
    - path: "src/components/entities/entity-data-table.tsx"
      provides: "Client Component with TanStack Table rendering"
      contains: "useReactTable"
    - path: "src/components/entities/columns.tsx"
      provides: "Typed column definitions for the entity table"
      contains: "ColumnDef"
    - path: "src/components/entities/status-badge.tsx"
      provides: "Color-coded badge components for status and risk"
      contains: "Badge"
  key_links:
    - from: "src/app/entities/page.tsx"
      to: "entity_health_summary"
      via: "Supabase server client SELECT"
      pattern: "from.*entity_health_summary"
    - from: "src/components/entities/entity-data-table.tsx"
      to: "src/components/entities/columns.tsx"
      via: "columns import"
      pattern: "import.*columns"
---

<objective>
Build the core entity registry table: install TanStack Table, add shadcn table component, create typed column definitions with color-coded status/risk badges, and wire the Server Component to fetch from entity_health_summary view.

Purpose: ENTY-01 requires users to view all 60+ entities with jurisdiction, status, type, and incorporation date. This plan delivers the visible table with all data columns and sorting.
Output: A working /entities page showing all entities in a dense, sortable TanStack Table with color-coded badges.
</objective>

<execution_context>
@/Users/harrykang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrykang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-entity-registry/03-RESEARCH.md

<interfaces>
<!-- Key types the executor needs from the existing codebase -->

From src/lib/db/types.ts:
```typescript
export type EntityPurpose = 'provider_key' | 'customer_entity' | 'hq'
export type EntityStatus = 'active' | 'dormant' | 'dissolving' | 'dissolved'
export type RiskLevel = 'critical' | 'warning' | 'ok'

export interface EntityHealthSummary {
  id: string
  name: string
  legal_name: string
  entity_type: string
  entity_purpose: EntityPurpose
  status: EntityStatus
  incorporation_date: string | null
  country_code: string
  country_name: string
  overdue_count: number
  due_soon_count: number
  open_requirements: number
  risk_level: RiskLevel
}
```

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

From src/components/ui/badge.tsx:
```typescript
// Uses cva with variants: default, secondary, destructive, outline, ghost, link
// Uses useRender pattern from @base-ui/react (NOT asChild)
export { Badge, badgeVariants }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install TanStack Table and add shadcn table component</name>
  <files>package.json, src/components/ui/table.tsx</files>
  <action>
Run these commands:
1. `npm install @tanstack/react-table` — install TanStack Table v8
2. `npx shadcn@latest add table` — add shadcn Table primitive component
3. `npx shadcn@latest add popover` — needed for faceted filters in Plan 02
4. `npx shadcn@latest add command` — needed for faceted filter search in Plan 02
5. `npx shadcn@latest add select` — needed for faceted filter dropdowns in Plan 02

Verify the table component was added to src/components/ui/table.tsx and @tanstack/react-table appears in package.json dependencies.
  </action>
  <verify>
    <automated>npm ls @tanstack/react-table && test -f src/components/ui/table.tsx && echo "OK"</automated>
  </verify>
  <done>@tanstack/react-table installed, shadcn table/popover/command/select components exist in src/components/ui/</done>
</task>

<task type="auto">
  <name>Task 2: Build entity data table with column definitions, badges, and server data fetch</name>
  <files>src/components/entities/columns.tsx, src/components/entities/entity-data-table.tsx, src/components/entities/status-badge.tsx, src/app/entities/page.tsx</files>
  <action>
Create four files:

**src/components/entities/status-badge.tsx** — Color-coded badge components:
- `StatusBadge` component: takes `status: EntityStatus`, renders Badge with:
  - `active` → green background (use custom Tailwind: `bg-emerald-100 text-emerald-800 dark:bg-emerald-900/30 dark:text-emerald-400`)
  - `dormant` → secondary variant (gray)
  - `dissolving` → destructive variant (red)
  - `dissolved` → outline variant (muted)
- `RiskBadge` component: takes `risk_level: RiskLevel`, renders Badge with:
  - `critical` → red (destructive variant with explicit text "At Risk")
  - `warning` → amber background (use custom Tailwind: `bg-amber-100 text-amber-800 dark:bg-amber-900/30 dark:text-amber-400`, text "Due Soon")
  - `ok` → green background (same emerald palette, text "Compliant")
- Both use the existing Badge component from `@/components/ui/badge` with className overrides for colors not in the default variants.

**src/components/entities/columns.tsx** — ColumnDef array:
- `"use client"` directive (column defs reference React components)
- Import `ColumnDef` from `@tanstack/react-table`
- Import `EntityHealthSummary` from `@/lib/db/types`
- Define `columns: ColumnDef<EntityHealthSummary>[]` with these columns:
  1. `name` — header "Entity", cell renders `row.original.name` as bold text with `legal_name` as muted subtitle below
  2. `country_name` — header "Jurisdiction", cell renders country flag emoji (derive from country_code) + country_name
  3. `entity_type` — header "Type", plain text
  4. `status` — header "Status", cell renders `<StatusBadge>`
  5. `risk_level` — header "Risk", cell renders `<RiskBadge>`
  6. `incorporation_date` — header "Incorporated", cell formats date using `date-fns` `format()` as "MMM yyyy" (e.g., "Jan 2020"), show "—" for null
  7. `overdue_count` — header "Overdue", cell renders number with red text if > 0
  8. `due_soon_count` — header "Due Soon", cell renders number with amber text if > 0
- All headers should use `enableSorting: true` by default (TanStack Table default)
- Column headers should be compact (no wrapping), use `text-xs font-medium text-muted-foreground` styling

**src/components/entities/entity-data-table.tsx** — Client Component:
- `"use client"` directive
- Props: `{ data: EntityHealthSummary[] }`
- Use `useReactTable` with: `data`, `columns`, `getCoreRowModel()`, `getSortedRowModel()`, `getFilteredRowModel()`, `getFacetedRowModel()`, `getFacetedUniqueValues()`
- State: `sorting` (SortingState), `columnFilters` (ColumnFiltersState), `globalFilter` (string)
- Render using shadcn `<Table>`, `<TableHeader>`, `<TableBody>`, `<TableRow>`, `<TableCell>` primitives
- Use `flexRender` to render header and cell content
- Table styling: compact/dense enterprise feel — use `text-sm` on cells, minimal padding (`py-2 px-3`), tight row height
- Row click: use `useRouter` from next/navigation, on row click `router.push(`/entities/${row.original.id}`)`
- Row hover: `cursor-pointer hover:bg-muted/50`
- Header click for sorting: toggle sort on click, show sort direction indicator (up/down arrow icon from lucide)
- Empty state: center-aligned "No entities found" message
- Below the table, show a row count: "{n} entities" in muted text

**src/app/entities/page.tsx** — Replace placeholder:
- Server Component (async function, no "use client")
- Import `createClient` from `@/lib/supabase/server`
- Fetch: `const { data: entities } = await supabase.from('entity_health_summary').select('*').order('name')`
- Type the result as `EntityHealthSummary[]`
- Render page header: "Entity Registry" title with "60+ legal entities across 20+ jurisdictions" subtitle
- Render `<EntityDataTable data={entities ?? []} />`
- Add metadata export: `export const metadata = { title: 'Entities | Hopae' }`

For the country flag emoji helper, create a simple utility function that converts a 2-letter country_code to regional indicator emoji (e.g., "US" -> flag emoji). This is a standard Unicode conversion: each letter maps to a regional indicator symbol (char code 0x1F1E6 + letter index).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && echo "TypeCheck OK"</automated>
  </verify>
  <done>
- /entities page renders a dense TanStack Table with all entity columns
- Status badges show color-coded risk and entity status
- Columns are sortable by clicking headers
- Each row is clickable (navigates to /entities/[id])
- TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no type errors
2. `npm run build` — builds successfully (page renders at build time as Server Component)
3. Visual: /entities page shows a table with 60+ rows, color-coded badges visible
</verification>

<success_criteria>
- Dense table visible at /entities with all 60+ entities
- Columns: entity name, jurisdiction (with flag), type, status badge, risk badge, incorporation date, overdue count, due-soon count
- Sorting works on all columns
- Status badges: red for at-risk/dissolving, amber for due-soon, green for compliant/active
- Rows clickable (navigate to /entities/[id])
- No horizontal scrolling needed at 1280px+
</success_criteria>

<output>
After completion, create `.planning/phases/03-entity-registry/03-01-SUMMARY.md`
</output>
