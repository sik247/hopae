---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/lib/supabase/server.ts
  - src/lib/supabase/client.ts
  - src/lib/db/types.ts
  - supabase/migrations/20260313000000_initial_schema.sql
  - .env.example
  - .env.local
autonomous: true
requirements: [DATA-01, DATA-02, DATA-05]

must_haves:
  truths:
    - "Next.js app bootstrapped with TypeScript, Tailwind v4, App Router, src directory"
    - "shadcn/ui initialized with new-york style and all Phase 1 components installed"
    - "Supabase server and browser clients are importable and correctly configured"
    - "SQL migration file defines all 7 tables + 1 view covering entities through Phase 9"
    - "Environment variables documented in .env.example for Supabase and Gemini"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "@supabase/ssr"
    - path: "src/lib/supabase/server.ts"
      provides: "Server-side Supabase client factory"
      exports: ["createClient"]
    - path: "src/lib/supabase/client.ts"
      provides: "Browser-side Supabase client factory"
      exports: ["createClient"]
    - path: "supabase/migrations/20260313000000_initial_schema.sql"
      provides: "Complete database schema"
      contains: "CREATE TABLE entities"
    - path: "src/lib/db/types.ts"
      provides: "TypeScript types for all database tables"
      exports: ["Entity", "Jurisdiction", "Director", "ComplianceRequirement", "Document", "IntercompanyAgreement", "Alert"]
    - path: ".env.example"
      provides: "Environment variable documentation"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
  key_links:
    - from: "src/lib/supabase/server.ts"
      to: ".env.local"
      via: "process.env.NEXT_PUBLIC_SUPABASE_URL"
      pattern: "process\\.env\\.NEXT_PUBLIC_SUPABASE"
    - from: "supabase/migrations/20260313000000_initial_schema.sql"
      to: "src/lib/db/types.ts"
      via: "schema defines tables, types mirror them"
      pattern: "CREATE TABLE"
---

<objective>
Bootstrap the Next.js project, install all dependencies, configure Supabase clients, and create the complete database schema migration.

Purpose: Establish the entire project foundation so that Plan 02 (navigation shell) and all future phases have a running app with typed database access.
Output: A bootable Next.js app with Supabase client utilities and the full SQL schema ready to apply.
</objective>

<execution_context>
@/Users/harrykang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrykang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Next.js project with all Phase 1 dependencies</name>
  <files>package.json, tsconfig.json, .env.example, .env.local, .gitignore</files>
  <action>
1. Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` in the project root (current directory is empty, so scaffold in place). If create-next-app requires an empty directory, use a temp name and move files. Answer "No" to Turbopack if prompted (stability concern for demo).

2. Immediately after scaffold, initialize shadcn/ui:
   ```
   npx shadcn@latest init
   ```
   Select: new-york style, neutral base color, CSS variables YES. This must run BEFORE any custom CSS edits.

3. Install shadcn components needed for Phase 1 navigation shell:
   ```
   npx shadcn@latest add sidebar button badge separator scroll-area tooltip dropdown-menu
   ```

4. Install Supabase packages:
   ```
   npm install @supabase/supabase-js @supabase/ssr
   ```

5. Install state/query/utility packages (wire up providers now, use in later phases):
   ```
   npm install @tanstack/react-query zustand sonner
   ```

6. Create `.env.example` with these exact contents:
   ```
   # Supabase (get from https://supabase.com/dashboard/project/_/settings/api)
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

   # Gemini AI (get from https://aistudio.google.com/apikey)
   GEMINI_API_KEY=your-gemini-api-key-here
   ```

7. Create `.env.local` with the same keys but empty values (user fills in real values):
   ```
   NEXT_PUBLIC_SUPABASE_URL=
   NEXT_PUBLIC_SUPABASE_ANON_KEY=
   GEMINI_API_KEY=
   ```

8. Verify `.gitignore` includes `.env*.local` (create-next-app should add this by default — confirm).

9. Verify `tsconfig.json` has `"strict": true` (create-next-app sets this by default — confirm).
  </action>
  <verify>
    <automated>cd /Users/harrykang/Desktop/hopae && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Next.js app scaffolded with all Phase 1 deps installed, shadcn/ui initialized with new-york style and sidebar component available, .env.example committed, .env.local created (gitignored), TypeScript strict mode on.</done>
</task>

<task type="auto">
  <name>Task 2: Create Supabase clients, database types, and SQL schema migration</name>
  <files>src/lib/supabase/server.ts, src/lib/supabase/client.ts, src/lib/db/types.ts, supabase/migrations/20260313000000_initial_schema.sql</files>
  <action>
1. Create `src/lib/supabase/server.ts` — the server-side Supabase client factory. Must `await cookies()` (Next.js 15+ async requirement). Exact implementation from research:
   ```typescript
   import { createServerClient } from '@supabase/ssr'
   import { cookies } from 'next/headers'

   export async function createClient() {
     const cookieStore = await cookies()
     return createServerClient(
       process.env.NEXT_PUBLIC_SUPABASE_URL!,
       process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
       {
         cookies: {
           getAll() { return cookieStore.getAll() },
           setAll(cookiesToSet) {
             try {
               cookiesToSet.forEach(({ name, value, options }) =>
                 cookieStore.set(name, value, options)
               )
             } catch {} // Safe to ignore in read-only Server Component context
           },
         },
       }
     )
   }
   ```

2. Create `src/lib/supabase/client.ts` — the browser-side Supabase client factory:
   ```typescript
   import { createBrowserClient } from '@supabase/ssr'

   export function createClient() {
     return createBrowserClient(
       process.env.NEXT_PUBLIC_SUPABASE_URL!,
       process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
     )
   }
   ```

3. Create `src/lib/db/types.ts` — hand-written TypeScript types mirroring the SQL schema. These are used until `supabase gen types` is available. Include all 7 table types:
   - `Jurisdiction` — id, country_code, country_name, filing_rules (Record<string, unknown>), currency, timezone, created_at
   - `Entity` — id, name, legal_name, entity_type, entity_purpose ('provider_key' | 'customer_entity' | 'hq'), jurisdiction_id, parent_entity_id (nullable), incorporation_date, registration_number, status ('active' | 'dormant' | 'dissolving' | 'dissolved'), banking_info, registered_agent, metadata, created_at, updated_at
   - `Director` — id, entity_id, full_name, role, nationality, start_date, end_date (nullable), is_current, created_at
   - `ComplianceRequirement` — id, entity_id, requirement_type, due_date, fiscal_year, status ('pending' | 'in_progress' | 'completed' | 'overdue'), notes, completed_at, created_at, updated_at
   - `Document` — id, entity_id, document_type, title, storage_path, source ('uploaded' | 'ai_drafted' | 'ai_extracted'), signature_status ('not_required' | 'pending' | 'signed'), signatories, extracted_data, created_at, updated_at
   - `IntercompanyAgreement` — id, entity_id, hq_entity_id, agreement_type, title, effective_date, expiry_date, governing_law, parties, key_terms, document_id, status ('active' | 'expired' | 'terminated' | 'draft'), created_at
   - `Alert` — id, entity_id, requirement_id, alert_type ('overdue' | 'due_soon' | 'at_risk' | 'info'), message, due_date, resolved, resolved_at, created_at
   - Also export `EntityHealthSummary` matching the SQL view columns.
   - Use string literal union types for status/type fields. Use `Record<string, unknown>` for JSONB fields (or define specific shapes where known, like `BankingInfo`, `RegisteredAgent`, `FilingRules`).

4. Create `supabase/migrations/20260313000000_initial_schema.sql` with the COMPLETE schema from the research document. This includes:
   - `pgcrypto` extension for UUID generation
   - 7 tables: jurisdictions, entities, directors, compliance_requirements, documents, intercompany_agreements, alerts
   - All indexes (11 indexes for common query patterns)
   - The `entity_health_summary` view
   - Key columns per Phase 1 success criteria: `entity_type`, `parent_entity_id` (self-ref FK), `metadata` JSONB on entities
   - `entity_purpose` column on entities for DATA-05 (provider_key | customer_entity | hq)
   - `filing_rules` JSONB on jurisdictions for DATA-02

   Use the exact SQL from the research document's "Complete Database Schema" section — it has been validated against the Phase 1 success criteria.
  </action>
  <verify>
    <automated>cd /Users/harrykang/Desktop/hopae && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Supabase server and client factories exist and type-check. Database types file exports all 7 table types + view type. SQL migration file contains the complete schema with all tables, indexes, and the entity_health_summary view. The migration is ready to apply to a Supabase project.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds (TypeScript compiles clean)
- `src/lib/supabase/server.ts` exports `createClient` using `await cookies()`
- `src/lib/supabase/client.ts` exports `createClient` using `createBrowserClient`
- `supabase/migrations/20260313000000_initial_schema.sql` contains CREATE TABLE for all 7 tables
- `.env.example` documents all 3 environment variables
- `src/lib/db/types.ts` exports Entity type with `entity_type`, `parent_entity_id`, `metadata` fields
</verification>

<success_criteria>
- The project bootstraps and `npm run build` passes
- All Phase 1 dependencies are installed (check package.json for @supabase/ssr, @tanstack/react-query, zustand, sonner)
- Supabase client files type-check correctly
- SQL schema covers all tables needed through Phase 9
- TypeScript types match the SQL schema 1:1
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-01-SUMMARY.md`
</output>
