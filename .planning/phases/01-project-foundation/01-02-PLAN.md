---
phase: 01-project-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/dashboard/page.tsx
  - src/app/entities/page.tsx
  - src/app/compliance/page.tsx
  - src/app/documents/page.tsx
  - src/components/layout/app-sidebar.tsx
  - src/components/layout/header.tsx
  - src/components/providers.tsx
autonomous: false
requirements: []

must_haves:
  truths:
    - "Enterprise sidebar navigation renders with Dashboard, Entities, Compliance, Documents links"
    - "Clicking sidebar links navigates between placeholder pages"
    - "Sidebar can be collapsed/expanded via trigger button or keyboard shortcut"
    - "npm run dev starts without errors and the app renders at localhost:3000"
    - "Root layout wraps entire app in SidebarProvider, QueryClientProvider, and Toaster"
  artifacts:
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Config-driven enterprise sidebar with nav items"
      min_lines: 30
    - path: "src/components/layout/header.tsx"
      provides: "Top header with sidebar trigger and page title"
      min_lines: 15
    - path: "src/app/layout.tsx"
      provides: "Root layout with SidebarProvider + providers"
      contains: "SidebarProvider"
    - path: "src/components/providers.tsx"
      provides: "Client-side provider wrapper (QueryClient, etc.)"
      exports: ["Providers"]
    - path: "src/app/dashboard/page.tsx"
      provides: "Placeholder dashboard page"
    - path: "src/app/entities/page.tsx"
      provides: "Placeholder entities page"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/app-sidebar.tsx"
      via: "import and render AppSidebar inside SidebarProvider"
      pattern: "AppSidebar"
    - from: "src/components/layout/app-sidebar.tsx"
      to: "src/app/dashboard/page.tsx"
      via: "Link href='/dashboard'"
      pattern: "href.*dashboard"
    - from: "src/app/layout.tsx"
      to: "src/components/providers.tsx"
      via: "import Providers, wrap children"
      pattern: "Providers"
---

<objective>
Build the enterprise navigation shell — sidebar, header, root layout with all providers, and placeholder route pages for all navigation targets.

Purpose: Give all future phases a working application shell to build within. The sidebar + layout is the persistent chrome for the entire app.
Output: A running app at localhost:3000 with navigable sidebar linking to Dashboard, Entities, Compliance, and Documents pages.
</objective>

<execution_context>
@/Users/harrykang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrykang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@.planning/phases/01-project-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 outputs — executor needs these to wire providers and layout correctly -->

shadcn/ui Sidebar components (installed in Plan 01):
- SidebarProvider, Sidebar, SidebarContent, SidebarHeader, SidebarFooter
- SidebarGroup, SidebarGroupLabel, SidebarMenu, SidebarMenuItem, SidebarMenuButton
- SidebarTrigger
- All imported from "@/components/ui/sidebar"

shadcn/ui Sonner (installed in Plan 01):
- Toaster imported from "@/components/ui/sonner" (NOT "sonner" directly)

lucide-react icons (installed with shadcn):
- Building2, Shield, FileText, LayoutDashboard from "lucide-react"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create providers wrapper, enterprise sidebar, header, and root layout</name>
  <files>src/components/providers.tsx, src/components/layout/app-sidebar.tsx, src/components/layout/header.tsx, src/app/layout.tsx</files>
  <action>
1. Create `src/components/providers.tsx` — a "use client" component that wraps children in `QueryClientProvider` from @tanstack/react-query. Create the QueryClient instance inside the component using `useState` to avoid sharing across requests. This is the single place for all client-side providers.

2. Create `src/components/layout/app-sidebar.tsx` — the enterprise sidebar. This is a "use client" component. Implementation:
   - Define a `NAV_ITEMS` config array with 4 entries:
     - { label: "Dashboard", href: "/dashboard", icon: LayoutDashboard }
     - { label: "Entities", href: "/entities", icon: Building2 }
     - { label: "Compliance", href: "/compliance", icon: Shield }
     - { label: "Documents", href: "/documents", icon: FileText }
   - Use `usePathname()` from next/navigation to determine active state
   - Render shadcn Sidebar with:
     - `SidebarHeader` containing "Hopae Ops" branding text (font-semibold, text-sm)
     - `SidebarContent` with a `SidebarGroup` containing `SidebarGroupLabel` "Navigation"
     - `SidebarMenu` mapping over NAV_ITEMS, each as `SidebarMenuItem` > `SidebarMenuButton asChild isActive={pathname.startsWith(item.href)}` > `Link`
     - Each menu button shows icon (size-4) + label span
   - Follow the exact pattern from the research document's Pattern 1

3. Create `src/components/layout/header.tsx` — a simple top header bar. Implementation:
   - "use client" component
   - Renders a `header` element with flex layout, items-center, border-b, h-14, px-4
   - Contains `SidebarTrigger` (from @/components/ui/sidebar) on the left for collapse/expand
   - Contains a separator (from @/components/ui/separator, orientation="vertical", className="h-6")
   - Contains a placeholder breadcrumb or page title area (just a div with "Hopae Entity Management" text for now)

4. Update `src/app/layout.tsx` — the root layout. Implementation:
   - Import `SidebarProvider` from "@/components/ui/sidebar"
   - Import `AppSidebar` from "@/components/layout/app-sidebar"
   - Import `Header` from "@/components/layout/header"
   - Import `Providers` from "@/components/providers"
   - Import `Toaster` from "@/components/ui/sonner"
   - Structure:
     ```
     <html lang="en">
       <body className={font classes + "flex min-h-screen"}>
         <Providers>
           <SidebarProvider defaultOpen={true}>
             <AppSidebar />
             <div className="flex-1 flex flex-col overflow-hidden">
               <Header />
               <main className="flex-1 overflow-auto p-6">
                 {children}
               </main>
             </div>
           </SidebarProvider>
           <Toaster />
         </Providers>
       </body>
     </html>
     ```
   - Keep the existing font imports from create-next-app (Geist Sans/Mono or Inter)
   - Keep the existing globals.css import
  </action>
  <verify>
    <automated>cd /Users/harrykang/Desktop/hopae && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Root layout renders SidebarProvider + AppSidebar + Header + Toaster. Sidebar shows 4 nav items with lucide icons. Header has SidebarTrigger for collapse/expand. QueryClientProvider wraps the app via Providers component. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create placeholder route pages for all navigation targets</name>
  <files>src/app/page.tsx, src/app/dashboard/page.tsx, src/app/entities/page.tsx, src/app/compliance/page.tsx, src/app/documents/page.tsx</files>
  <action>
1. Update `src/app/page.tsx` to redirect to /dashboard:
   ```typescript
   import { redirect } from 'next/navigation'
   export default function Home() {
     redirect('/dashboard')
   }
   ```

2. Create `src/app/dashboard/page.tsx` — placeholder Server Component:
   - Show a heading "Dashboard" (text-2xl font-bold)
   - Show a paragraph: "Portfolio overview and compliance health signals — coming in Phase 7."
   - Use a simple Card component (from shadcn) with subtle styling to make it look intentional, not broken

3. Create `src/app/entities/page.tsx` — placeholder:
   - Heading "Entity Registry"
   - Paragraph: "60+ legal entities across 20+ jurisdictions — coming in Phase 3."

4. Create `src/app/compliance/page.tsx` — placeholder:
   - Heading "Compliance Calendar"
   - Paragraph: "Deadline tracking and risk alerts — coming in Phase 6."

5. Create `src/app/documents/page.tsx` — placeholder:
   - Heading "Documents"
   - Paragraph: "AI drafting, extraction, and signature routing — coming in Phase 8."

All placeholder pages should follow the same visual pattern: a centered card with heading + description + a subtle icon or badge indicating the target phase. Keep them simple — 15-25 lines each. They are Server Components (no "use client").
  </action>
  <verify>
    <automated>cd /Users/harrykang/Desktop/hopae && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 navigation targets have working pages. Root "/" redirects to "/dashboard". Each page displays its name and future-phase description. Clicking sidebar links navigates between pages without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Phase 1 application shell</name>
  <files>N/A</files>
  <action>
Human verifies the complete Phase 1 application shell works end-to-end.

What was built: Next.js app with enterprise sidebar navigation, Supabase client utilities, complete database schema migration, and placeholder pages for all navigation targets.

Verification steps:
1. Run `npm run dev` in the project root
2. Open http://localhost:3000 — should redirect to /dashboard
3. Verify the sidebar shows 4 items: Dashboard, Entities, Compliance, Documents with icons
4. Click each sidebar item — verify navigation works and each page shows its placeholder content
5. Click the sidebar trigger (hamburger icon in header) — verify sidebar collapses and expands
6. Try keyboard shortcut Cmd+B (Mac) — verify sidebar toggles
7. Verify the SQL migration file exists at supabase/migrations/20260313000000_initial_schema.sql
8. Verify .env.example exists with the 3 required environment variables
  </action>
  <verify>Human confirms all 8 verification steps pass</verify>
  <done>User types "approved" — Phase 1 application shell verified working</done>
</task>

</tasks>

<verification>
- `npm run build` passes (TypeScript compiles clean, no warnings)
- `npm run dev` starts without errors
- Browser shows sidebar navigation at localhost:3000
- All 4 routes render their placeholder pages
- Sidebar collapse/expand works via trigger button and Cmd+B
- Root "/" redirects to "/dashboard"
</verification>

<success_criteria>
- Enterprise sidebar renders with Dashboard, Entities, Compliance, Documents navigation
- Active route is highlighted in sidebar
- Sidebar collapses/expands smoothly
- All placeholder pages are accessible
- Root layout includes SidebarProvider, QueryClientProvider, and Toaster
- `npm run build` and `npm run dev` both succeed
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-02-SUMMARY.md`
</output>
