---
phase: 02-seed-data
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - scripts/seed.ts
  - scripts/validate-seed.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]

must_haves:
  truths:
    - "At least 3 compliance requirements have status 'overdue' with due_date before 2026-03-13"
    - "At least 5 compliance requirements are pending with due_date within 30 days of 2026-03-13"
    - "At least 2 entities have status 'dissolving'"
    - "At least 3 unresolved alerts with alert_type 'overdue' exist"
    - "At least 5 unresolved alerts with alert_type 'due_soon' exist"
    - "At least 2 unresolved alerts with alert_type 'at_risk' exist for dissolving entities"
    - "Every non-HQ entity has at least 1 intercompany agreement with Luxembourg HQ"
    - "npm run db:seed populates all 7 tables and validate-seed passes"
  artifacts:
    - path: "scripts/seed.ts"
      provides: "Complete seed script with all 7 tables"
      min_lines: 800
    - path: "scripts/validate-seed.ts"
      provides: "Post-seed validation covering all DATA requirements"
      min_lines: 50
  key_links:
    - from: "scripts/seed.ts"
      to: "compliance_requirements table"
      via: "supabase.from('compliance_requirements').insert"
      pattern: "from\\('compliance_requirements'\\)\\.insert"
    - from: "scripts/seed.ts"
      to: "alerts table"
      via: "supabase.from('alerts').insert"
      pattern: "from\\('alerts'\\)\\.insert"
    - from: "scripts/seed.ts"
      to: "intercompany_agreements table"
      via: "supabase.from('intercompany_agreements').insert"
      pattern: "from\\('intercompany_agreements'\\)\\.insert"
    - from: "scripts/validate-seed.ts"
      to: "scripts/seed.ts"
      via: "validates data inserted by seed"
      pattern: "from\\('entities'\\)\\.select"
---

<objective>
Seed compliance requirements with dramatic tension (overdue, due-soon, healthy), intercompany agreements for all entities, and alerts. Then create a validation script that proves all DATA requirements are met.

Purpose: The compliance deadlines and alerts are what drive the demo's opening hook — "3 entities at risk" on the dashboard. Without this data, later phases (compliance engine, dashboard) have nothing to display. The validation script ensures seed data correctness.
Output: Complete seed script populating all 7 tables, plus validation script confirming all 5 DATA requirements.
</objective>

<execution_context>
@/Users/harrykang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harrykang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-seed-data/02-RESEARCH.md
@.planning/phases/02-seed-data/02-01-SUMMARY.md

@src/lib/db/types.ts
@supabase/migrations/20260313000000_initial_schema.sql
</context>

<interfaces>
<!-- Key types from src/lib/db/types.ts -->

```typescript
export type ComplianceStatus = 'pending' | 'in_progress' | 'completed' | 'overdue'

export interface ComplianceRequirement {
  id: string
  entity_id: string
  requirement_type: string
  due_date: string
  fiscal_year: number | null
  status: ComplianceStatus
  notes: string | null
  completed_at: string | null
}

export type AgreementStatus = 'active' | 'expired' | 'terminated' | 'draft'

export interface IntercompanyAgreement {
  id: string
  entity_id: string
  hq_entity_id: string
  agreement_type: string
  title: string
  effective_date: string | null
  expiry_date: string | null
  governing_law: string | null
  parties: AgreementParty[]
  key_terms: AgreementKeyTerms
  document_id: string | null
  status: AgreementStatus
}

export type AlertType = 'overdue' | 'due_soon' | 'at_risk' | 'info'

export interface Alert {
  id: string
  entity_id: string
  requirement_id: string | null
  alert_type: AlertType
  message: string
  due_date: string | null
  resolved: boolean
  resolved_at: string | null
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Seed compliance requirements and intercompany agreements</name>
  <files>scripts/seed.ts</files>
  <action>
Add compliance_requirements and intercompany_agreements data to `scripts/seed.ts`:

**COMPLIANCE_REQUIREMENTS array:**
Use the `daysFromSeed()` helper and SEED_DATE = 2026-03-13 for all date calculations.

Seed 2-3 compliance requirements per entity (annual_filing, tax_return, agent_renewal) based on that entity's jurisdiction filing_rules. Total: ~150-190 rows.

**Overdue items (at least 3, status = 'overdue'):**
Per RESEARCH.md dramaturgy map:
- Hopae Brasil Ltda. (BR) — annual_filing — due_date: '2026-01-31' — fiscal_year: 2025 — notes: 'Annual financial statements for FY2025 - Brazil DNRC filing. 41 days overdue.'
- Hopae Poland Sp. z o.o. (PL) — annual_filing — due_date: '2026-02-28' — fiscal_year: 2025 — notes: 'FY2025 annual accounts overdue. 13 days overdue.'
- Hopae Belgium Ghent Branch (BE, dissolving) — agent_renewal — due_date: '2026-02-01' — fiscal_year: 2025 — notes: 'Registered agent renewal lapsed. Branch dissolving.'

**Due-soon items (at least 5, status = 'pending', due_date within 30 days of 2026-03-13):**
- 주식회사 Hopae Korea (KR) — tax_return — due_date: '2026-03-31' — 18 days
- Hopae Poland Sp. z o.o. (PL) — tax_return — due_date: '2026-03-31' — 18 days
- Hopae India Private Limited (IN) — annual_filing — due_date: '2026-03-31' — 18 days (India FYE Mar 31)
- Hopae Finland Oy (FI) — tax_return — due_date: '2026-04-10' — 28 days
- Hopae KB Kookmin ID (KR customer) — tax_return — due_date: '2026-03-31' — 18 days

**Healthy items (majority):**
For remaining entities, seed 1-3 requirements with due dates 60-365 days out (status = 'pending') or completed prior year filings (status = 'completed', completed_at set). This creates a realistic portfolio where most entities are fine.

Generate compliance requirement IDs with format `d0000000-0000-0000-0000-000000000NNN` so they can be referenced by alerts.

**INTERCOMPANY_AGREEMENTS array (~63 rows, one per non-HQ entity):**
Per RESEARCH.md agreement types and distribution:
- Every non-HQ entity gets at least 1 agreement with Luxembourg HQ
- agreement_type distribution: 23 service_agreement (all provider_key entities), 15 ip_license, 10 management_fee, 8 loan_agreement, 7 data_processing (spread across customer entities and branches)
- effective_date: shortly after entity incorporation_date
- expiry_date: null for evergreen agreements; set for loan_agreement types (5-year term)
- governing_law: 'Luxembourg' for all
- parties JSONB: `[{ name: entity_legal_name, role: 'Service Recipient', entity_id: entity_id }, { name: 'Hopae S.a r.l.', role: 'Service Provider', entity_id: HQ_ID }]`
- key_terms JSONB: per RESEARCH.md patterns (fee_amount: 50000 for service, 500000 for loans, etc.)
- status: 'active' for most; 'expired' for 2 dissolving entities + 3 others; 'draft' for 3 newest entities

Generate agreement IDs with format `e0000000-0000-0000-0000-000000000NNN`.

Add both inserts to main() after directors, before alerts. Insert compliance_requirements first (alerts reference them).
  </action>
  <verify>
    <automated>npx tsx scripts/seed.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
- 150+ compliance_requirements inserted
- At least 3 with status 'overdue' and due_date before 2026-03-13
- At least 5 with status 'pending' and due_date within 30 days of 2026-03-13
- 62 intercompany agreements inserted (one per non-HQ entity)
- All agreements reference correct entity_id and hq_entity_id
  </done>
</task>

<task type="auto">
  <name>Task 2: Seed alerts and create validation script</name>
  <files>scripts/seed.ts, scripts/validate-seed.ts</files>
  <action>
**Part A: Add ALERTS array to scripts/seed.ts**

CRITICAL: Alerts must be seeded SEPARATELY from compliance_requirements (per planning context). The alerts table drives the dashboard red-badge count.

Create alerts for every overdue and due-soon compliance requirement, plus dissolution-risk alerts:

**Overdue alerts (at least 3, alert_type = 'overdue'):**
For each overdue compliance_requirement, create a matching alert:
- entity_id: same as the compliance_requirement
- requirement_id: the compliance_requirement's deterministic ID
- alert_type: 'overdue'
- message: Descriptive, e.g., 'Annual filing for FY2025 is 41 days overdue. Immediate action required.'
- due_date: same as the compliance_requirement's due_date
- resolved: false

**Due-soon alerts (at least 5, alert_type = 'due_soon'):**
For each due-soon compliance_requirement:
- alert_type: 'due_soon'
- message: e.g., 'Tax return due in 18 days (2026-03-31). Prepare filing documents.'
- resolved: false

**Dissolution-risk alerts (at least 2, alert_type = 'at_risk'):**
For the 2 dissolving entities:
- requirement_id: null (not tied to a specific compliance requirement)
- alert_type: 'at_risk'
- message: e.g., 'Entity in dissolution process. Ensure all wind-down obligations are met.'
- due_date: null
- resolved: false

Total: at least 10 alert records. Generate IDs with format `f0000000-0000-0000-0000-000000000NNN`.

Add alert insert to main() as the LAST insert (after compliance_requirements and intercompany_agreements).

Update the summary log at end of main() to print counts for all 7 tables.

**Part B: Create scripts/validate-seed.ts**

Create a validation script that queries the seeded database and asserts all DATA requirements are met. Use the same Supabase client setup (dotenv + service role key).

Checks to implement (exit with code 1 if any fail, print clear pass/fail per check):

1. DATA-01: `SELECT COUNT(*) FROM entities` >= 60
2. DATA-01: `SELECT COUNT(DISTINCT jurisdiction_id) FROM entities` >= 20
3. DATA-01: Spot-check legal name suffixes: query 3 entities (JP, DE, SG) and verify legal_name contains correct suffix (株式会社, GmbH, Pte. Ltd.)
4. DATA-02: `SELECT COUNT(*) FROM jurisdictions` = 23
5. DATA-02: Every jurisdiction has filing_rules with annual_filing_month and fiscal_year_end_month (query and check JSONB fields)
6. DATA-03: `SELECT COUNT(*) FROM compliance_requirements WHERE status = 'overdue'` >= 3
7. DATA-03: `SELECT COUNT(*) FROM compliance_requirements WHERE status = 'pending' AND due_date BETWEEN '2026-03-13' AND '2026-04-12'` >= 5
8. DATA-03: `SELECT COUNT(*) FROM entities WHERE status = 'dissolving'` >= 2
9. DATA-03: `SELECT COUNT(*) FROM alerts WHERE alert_type = 'overdue' AND resolved = false` >= 3
10. DATA-03: `SELECT COUNT(*) FROM alerts WHERE alert_type = 'due_soon' AND resolved = false` >= 5
11. DATA-03: `SELECT COUNT(*) FROM alerts WHERE alert_type = 'at_risk' AND resolved = false` >= 2
12. DATA-04: Every entity has at least 1 director: `SELECT e.id FROM entities e LEFT JOIN directors d ON d.entity_id = e.id WHERE d.id IS NULL` returns 0 rows
13. DATA-04: Every non-HQ entity has at least 1 intercompany agreement: `SELECT e.id FROM entities e LEFT JOIN intercompany_agreements ia ON ia.entity_id = e.id WHERE e.entity_purpose != 'hq' AND ia.id IS NULL` returns 0 rows
14. DATA-05: entity_purpose counts: hq=1, provider_key>=23, customer_entity>=28

Print summary at end: "X/14 checks passed" and list any failures with details.

Add to package.json scripts: `"db:validate": "tsx scripts/validate-seed.ts"`
  </action>
  <verify>
    <automated>npm run db:seed && npx tsx scripts/validate-seed.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
- At least 10 alert records seeded (3 overdue + 5 due_soon + 2 at_risk minimum)
- All alerts are unresolved (resolved = false)
- Overdue alerts reference their compliance_requirement IDs
- validate-seed.ts runs and reports 14/14 checks passed
- Full seed is idempotent: running `npm run db:seed` twice produces identical results
  </done>
</task>

</tasks>

<verification>
Run the full seed and validation sequence:
```bash
npm run db:seed && npm run db:validate
```
All 14 validation checks must pass. The seed must be idempotent (re-running produces same result).
</verification>

<success_criteria>
- `npm run db:seed` populates all 7 tables without errors
- `npm run db:validate` reports 14/14 checks passed
- Dramatic tension: 3+ overdue, 5+ due-soon within 30 days, 2+ dissolving entities
- Every overdue/due-soon compliance requirement has a matching alert
- Every non-HQ entity has at least 1 intercompany agreement with Luxembourg HQ
- Dashboard-critical data: alerts table has 10+ unresolved alerts for Phase 7
</success_criteria>

<output>
After completion, create `.planning/phases/02-seed-data/02-02-SUMMARY.md`
</output>
